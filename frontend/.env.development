# Only `next dev` reads this. It serves the frontend on port 3000, so API calls
# have to cross to the backend on 8000. The production build leaves this unset
# and calls its own origin, because FastAPI serves both.
#
# Not a secret — it is a localhost address.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
