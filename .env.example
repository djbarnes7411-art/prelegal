# Copy to .env and fill in. .env is gitignored.

# Needed by the AI chat that drafts the agreement. Without it the app still
# starts and serves, but the chat cannot answer — and the chat is the only way
# to fill a document in. Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=
